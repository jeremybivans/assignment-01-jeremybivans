<%= form_with(model: teacher_office) do |form| %>
  <% if teacher_office.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(teacher_office.errors.count, "error") %> prohibited this teacher_office from being saved:</h2>

      <ul>
        <% teacher_office.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :building_name %>
    <%= form.text_field :building_name %>
  </div>

  <div class="field">
    <%= form.label :room_number %>
    <%= form.number_field :room_number %>
  </div>

  <div class="field">
    <%= form.label :floor_number %>
    <%= form.number_field :floor_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
